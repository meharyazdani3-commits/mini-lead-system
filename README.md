# Mini Lead Distribution System

Next.js + Prisma + PostgreSQL lead capture system with email notifications.

## Setup
1. `npm install`
2. Create `.env` with your `DATABASE_URL` and email credentials
3. `npx prisma migrate dev`
4. `npm run dev`

## Tech Stack
- Next.js 14
- Prisma ORM
- PostgreSQL
- Nodemailer
